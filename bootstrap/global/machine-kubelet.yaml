machine:
  kubelet:
    extraConfig:
      serializeImagePulls: false
    nodeIP:
      validSubnets:
        - 10.1.80.0/24
    # extraMounts:
    #   - destination: /var/local
    #     type: bind
    #     source: /var/local
    #     options:
    #       - bind
    #       - rshared
    #       - rw
