# Let kube-system workloads read the Talos API
# (os:reader — inventory/upgrade tooling like talos-backup or CCM later).
machine:
  features:
    kubernetesTalosAPIAccess:
      enabled: true
      allowedRoles:
        - os:reader
      allowedKubernetesNamespaces:
        - kube-system
