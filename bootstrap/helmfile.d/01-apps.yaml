---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile

# helmfile --file bootstrap/helmfile.d/01-apps.yaml sync

helmDefaults:
  wait: true
  waitForJobs: true
  cleanupOnFail: true

releases:
  - name: cilium
    namespace: kube-system
    # chart: https://helm.cilium.io
    chart: oci://quay.io/cilium/charts/cilium
    version: 1.20.1
    values:
      - ./templates/values.yaml.gotmpl
      # Bootstrap-only override: hubble-relay needs cluster DNS, coredns
      # `needs` cilium, and helmDefaults wait:true — enabling hubble here
      # deadlocks the bootstrap. Flux flips hubble on afterwards from the
      # HelmRelease values (no manual toggle needed anymore).
      - hubble:
          enabled: false
          relay:
            enabled: false
          ui:
            enabled: false

  - name: coredns
    namespace: kube-system
    chart: oci://ghcr.io/coredns/charts/coredns
    # chart: https://coredns.github.io/helm
    version: 1.47.0
    values:
      - ./templates/values.yaml.gotmpl
    needs:
      - kube-system/cilium

  # - name: spegel
  #   namespace: kube-system
  #   chart: oci://ghcr.io/spegel-org/helm-charts/spegel
  #   version: 0.5.1
  #   values:
  #     - ./templates/values.yaml.gotmpl
  #   needs:
  #     - kube-system/coredns

  - name: cert-manager
    namespace: cert-manager
    chart: oci://quay.io/jetstack/charts/cert-manager
    version: v1.21.1
    values:
      - ./templates/values.yaml.gotmpl
    needs:
      - kube-system/coredns

  - name: flux-operator
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
    version: 0.58.1
    values:
      - "./templates/values.yaml.gotmpl"
    needs:
      - "cert-manager/cert-manager"

  - name: flux-instance
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
    version: 0.58.1
    values:
      - "./templates/values.yaml.gotmpl"
    needs:
      - "flux-system/flux-operator"
