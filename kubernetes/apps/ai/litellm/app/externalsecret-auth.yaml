---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
# App-level auth. secretKey = env var name (loaded via envFrom).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: litellm-auth
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-fields # custom fields on the item
  target:
    name: litellm-auth
    creationPolicy: Owner
  data:
    # Admin UI login (user admin + master key) and virtual-key minting key.
    - secretKey: LITELLM_MASTER_KEY
      remoteRef:
        key: 5a8919a2-21ed-4211-bcb4-0f55bee34729 # vaultwarden: litellm-master-key (field: master_key)
        property: master_key
