---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hindsight-cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi
  resources:
    requests:
      storage: 5Gi # else embedder/reranker models re-download every restart
