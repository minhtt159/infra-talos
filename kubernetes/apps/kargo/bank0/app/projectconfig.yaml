---
# staging promotes itself; production waits for a human.
apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: bank0
  namespace: bank0
spec:
  promotionPolicies:
    - stageSelector:
        name: staging
      autoPromotionEnabled: true
