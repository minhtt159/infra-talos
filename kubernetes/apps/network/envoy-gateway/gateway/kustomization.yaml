---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # per-gateway config (each dir owns its GatewayClass + EnvoyProxy + policies)
  - ./external
  - ./internal
  # shared: the http->https redirect binds both gateways' :80 listeners
  - ./httpredirect.yaml
