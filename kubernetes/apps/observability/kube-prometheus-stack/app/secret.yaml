---
# Thanos Ruler `--query.config`. Not sensitive; Secret because CRD wants one.
apiVersion: v1
kind: Secret
metadata:
  name: thanos-ruler-query-config
stringData:
  query-configs.yaml: |
    - scheme: https
      static_configs:
        - promeo.${SECRET_DOMAIN}
