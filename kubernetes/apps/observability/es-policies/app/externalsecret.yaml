---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: es-index-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-fields
  target:
    name: es-index-manager
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef:
        key: REPLACE-WITH-UUID # vaultwarden: homelab-talos/es-index-manager (field: api-key = "encoded" from _security/api_key)
        property: api-key
