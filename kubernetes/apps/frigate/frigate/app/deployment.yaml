---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: &name frigate
spec:
  selector:
    matchLabels:
      app: *name
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: *name
    spec:
      restartPolicy: Always
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: capability
                    operator: In
                    values:
                      - coral
      containers:
        - name: frigate
          image: ghcr.io/blakeblackshear/frigate:0.17.0-rk@sha256:b6a4aa8be933979dfe808aac6a960f1f3a5be34187ddfa6a0fa693829e85487a # stable-rk
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 400Mi
            # limits:
            #   cpu: 100m
            #   memory: 400Mi
          ports:
            - name: webui
              containerPort: 8971
            - name: internal
              containerPort: 5000
            - name: rstp
              containerPort: 8554
            - name: webrtc
              containerPort: 8555
          volumeMounts:
            - name: coral
              mountPath: /dev/apex_0
            - name: config
              mountPath: /config
            - name: media
              mountPath: /media/frigate
            - name: shm-volume
              mountPath: /dev/shm
          securityContext:
            privileged: true
          # securityContext:
          #   capabilities:
          #     drop:
          #       - "ALL"
          #     add:
          #       - "NET_BIND_SERVICE"
          #   seccompProfile:
          #     type: "RuntimeDefault"
          #   runAsUser: 44
          #   runAsGroup: 44
          #   runAsNonRoot: true
          #   allowPrivilegeEscalation: false
      volumes:
        - name: coral
          hostPath:
            path: /dev/apex_0
        - name: config
          persistentVolumeClaim:
            claimName: frigate-config
        - name: media
          persistentVolumeClaim:
            claimName: frigate-media
        - name: shm-volume
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi
