---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./networks.yaml
  - ./bgp.yaml
  - ./grafanadashboard.yaml
  - ./prometheusrule.yaml
  # Disabled July 2026: hostFirewall + policyAuditMode gone from values; these
  # CCNPs would enforce immediately. Re-enable with a policy rollout plan.
  # - ./host-firewall.yaml
  # - ./default-policies.yaml
