---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/postgresql.cnpg.io/database_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: hindsight
spec:
  name: hindsight
  owner: hindsight
  cluster:
    name: postgres
  extensions:
    - name: vector # pgvector, from the -standard image
      ensure: present
